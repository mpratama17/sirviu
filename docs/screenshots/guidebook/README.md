# Guidebook screenshots

Selected highlights backing `SIRVIU-Panduan-Pengguna.docx` (kept outside
this repo, in the Porto folder) — the full docx has more screenshots per
role than kept here. Covers: login, pilih peran (onboarding), upload
dokumen, panel aksi reviewer + modal reject, panel aksi Operator, dokumen
final, timeline lengkap, dashboard Admin, dan Audit Trail. Taken against
a disposable demo document and `@sirviu.local` test accounts, all since
deleted.

Separate from the curated set in `docs/screenshots/` used by the root
`README.md`.
